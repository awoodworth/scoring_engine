= header "Events", Event, icon: :trophy

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Available At
      %th Unavailable At
      %th.center Current
      %th

  %tbody
    - @events.each do |event|
      %tr
        %td= event.name
        %td= event.available_at.strftime("%m/%d/%Y %H:%M")
        %td= event.unavailable_at.strftime("%m/%d/%Y %H:%M")
        %td.center= event.available? ? 'yes' : 'no'
        %td.right= object_actions event
